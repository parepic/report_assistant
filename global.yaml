data_path: "data/"
output_path: "output/"

report_id: "apple_10-k-item1a"

OLLAMA_URL: "http://localhost:11434"
QDRANT_URL: "http://localhost:6333"
LLM_MODEL: "gpt-4o-mini"

# Chunking variables
chunk_strategy: {
  "embed_model": "nomic-embed-text",
  "method":"sentence_metadata",
  "chunk_size": 3,
  "overlap": 1
}

# Retrieval variables
top_k: 4

# Test variables
print_chunks: True