"""Test package marker enabling cross-module helper imports.

`tests.helpers` is shared by multiple test modules and relies on package-style
imports; this file makes the `tests` directory importable as a package.
"""
